'use strict';

/**
 * A set of functions called "actions" for `brand`
 */

const { createCoreController } = require("@strapi/strapi").factories;

export default createCoreController("api::brand.brand");